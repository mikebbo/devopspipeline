name: CI_Pipeline
on: 
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Chechout code
      uses: actions/checkout@v3
      with:
        node-version: 16
      name: Install dependencies
      run: npm install
      name: Run tests
      run: npm test
      